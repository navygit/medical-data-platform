# Core runtime dependencies.
#
# pyproject.toml is the source of truth; this file exists for `pip install -r`
# convenience and for tools that expect it. Prefer:
#     pip install -e ".[dev]"
#
# Deliberately excludes torch/MONAI. Ingestion, QC, governance and versioning --
# the substance of this repository -- run without a 2 GB deep-learning stack.
# For training:  pip install -e ".[ml]"

numpy>=1.26
pandas>=2.1
pyarrow>=14.0
pydantic>=2.5
pyyaml>=6.0
nibabel>=5.2
pydicom>=2.4
matplotlib>=3.8
plotly>=5.18
scikit-learn>=1.4
jinja2>=3.1
